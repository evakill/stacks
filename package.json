{
  "name": "stacks",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "express": "^4.17.1",
    "mongoose": "^5.9.28",
    "nodemon": "^2.0.4",
    "socket.io": "^2.3.0"
  },
  "scripts": {
    "start": "node app.js",
    "heroku-postbuild": "cd client && yarn && yarn run start",
    "dev": "nodemon app.js & (cd client && yarn run build)"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
